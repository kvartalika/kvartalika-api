ktor:
  application:
    modules:
      - kvartalica.ApplicationKt.module
  deployment:
    port: 8080
    host: localhost
  development: true
jwt:
  domain: "http://localhost:8080/"
  audience: "jwt-audience"
  realm: "ktor sample app"
postgres:
  url: "jdbc:postgresql://localhost/default"
  user: username
  password: password